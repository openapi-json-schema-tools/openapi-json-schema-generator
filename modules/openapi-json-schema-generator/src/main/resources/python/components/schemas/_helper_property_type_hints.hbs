{{#if requiredProperties}}

    {{#each requiredProperties}}
        {{#if @key.isValid}}
            {{#with this}}
                {{#if refInfo.refClass}}
{{@key.original}}: '{{> components/schemas/_helper_refclass_with_module }}'
                {{else}}
                    {{#if name}}
                        {{#if schemaIsFromAdditionalProperties}}
{{@key.original}}: Schema_.{{name.camelCase}}
                        {{else}}
{{@key.original}}: Schema_.Properties.{{name.camelCase}}
                        {{/if}}
                    {{else}}
{{@key.original}}: schemas.AnyTypeSchema
                    {{/if}}
                {{/if}}
            {{/with}}
        {{/if}}
    {{/each}}
{{/if}}
{{> components/schemas/_helper_getitems }}