{{#unless isArray}}{{#unless refInfo.refClass}}{{#if enumNameToValue}} must be one of [{{#each enumNameToValue}}{{#eq this "schemas.NoneClass.NONE"}}None{{else}}{{#eq this "schemas.BoolClass.TRUE"}}True{{else}}{{#eq this "schemas.BoolClass.FALSE"}}False{{else}}{{{this}}}{{/eq}}{{/eq}}{{/eq}}, {{/each}}]{{/if}}{{#if defaultValue}}{{#unless requiredProperties}} if omitted the server will use the default value of {{{defaultValue}}}{{/unless}}{{/if}}{{#eq format "uuid"}} value must be a uuid{{/eq}}{{#eq format "date"}} value must conform to RFC-3339 full-date YYYY-MM-DD{{/eq}}{{#eq format "date-time"}} value must conform to RFC-3339 date-time{{/eq}}{{#eq format "number"}} value must be numeric and storable in decimal.Decimal{{/eq}}{{#eq format "int32"}} value must be a 32 bit integer{{/eq}}{{#eq format "int64"}} value must be a 64 bit integer{{/eq}}{{#eq format "double"}} value must be a 64 bit float{{/eq}}{{#eq format "float"}} value must be a 32 bit float{{/eq}}{{/unless}}{{/unless}}