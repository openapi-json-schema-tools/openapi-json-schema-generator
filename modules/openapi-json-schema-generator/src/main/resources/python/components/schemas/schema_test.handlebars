# coding: utf-8

{{>partial_header}}

import unittest

import {{packageName}}
{{#with schema}}
from {{packageName}}.components.schema.{{componentModule}} import {{name.getCamelCaseName}}
from {{packageName}} import configuration


class Test{{name.getCamelCaseName}}(unittest.TestCase):
    """{{name.getCamelCaseName}} unit test stubs"""
    configuration_ = configuration.Configuration()

{{#each testCases}}
{{#with this }}
    def test_{{@key}}_{{#if valid}}passes{{else}}fails{{/if}}(self):
        # {{description}}
{{#if valid}}
        {{name.getCamelCaseName}}.from_openapi_data_(
{{#with data}}
            {{> components/schemas/payload_renderer endChar=',' }}
{{/with}}
            configuration_=self.configuration_
        )
{{else}}
        with self.assertRaises(({{packageName}}.ApiValueError, {{packageName}}.ApiTypeError)):
            {{name.getCamelCaseName}}.from_openapi_data_(
{{#with data}}
                {{> components/schemas/payload_renderer endChar=','}}
{{/with}}
                configuration_=self.configuration_
            )
{{/if}}
{{/with}}

{{/each}}
{{/with}}

if __name__ == '__main__':
    unittest.main()
