{{#each getRequiredProperties}}
{{#if @key.nameIsValid}}
{{#with this}}
{{#if refInfo.refClass}}
{{@key.getName}}: '{{> components/schemas/refclass_with_module }}'
{{else}}
    {{#if name}}
        {{#if schemaIsFromAdditionalProperties}}
{{@key.getName}}: Schema_.{{name.getCamelCaseName}}
        {{else}}
{{@key.getName}}: Schema_.Properties.{{name.getCamelCaseName}}
        {{/if}}
    {{else}}
{{@key.getName}}: schemas.AnyTypeSchema
    {{/if}}
{{/if}}
{{/with}}
{{/if}}
{{/each}}