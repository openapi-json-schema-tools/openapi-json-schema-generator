{{#if requiredProperties}}
    {{#each requiredProperties}}
        {{#with this}}
            {{#if refInfo.refClass}}

@property
{{> components/schemas/_helper_schema_output_ref_type fullRefModule="" mode="def_property" endChar=":" }}
    return self.__getitem__("{{{@key.original}}}")
            {{else}}
                {{#if jsonPathPiece}}

@property
{{> components/schemas/_helper_schema_output_type mode="def_property" fullRefModule=null endChar=":" }}
    return self.__getitem__("{{{@key.original}}}")
                {{else}}
                    {{! for when additionalProperties is unset, use OUTPUT_BASE_TYPES }}

@property
def {{@key.original}}(self) -> schemas.OUTPUT_BASE_TYPES:
    return self.__getitem__("{{{@key.original}}}")
                {{/if}}
            {{/if}}
        {{/with}}
    {{/each}}
{{/if}}
{{#if optionalProperties}}
    {{#each optionalProperties}}
        {{#with this}}
            {{#if refInfo.refClass}}

@property
{{> components/schemas/_helper_schema_output_ref_type fullRefModule="" mode="def_property" endChar=":" }}
    return self.__getitem__("{{{@key.original}}}")
            {{else}}
                {{#if jsonPathPiece}}

@property
{{> components/schemas/_helper_schema_output_type mode="def_property" fullRefModule=null endChar=":" }}
    return self.__getitem__("{{{@key.original}}}")
                {{else}}
                    {{! for when additionalProperties is unset, use OUTPUT_BASE_TYPES }}

@property
def {{@key.snakeCase}}(self) -> schemas.OUTPUT_BASE_TYPES:
    return self.__getitem__("{{{@key.original}}}")
                {{/if}}
            {{/if}}
        {{/with}}
    {{/each}}
{{/if}}
{{#eq additionalProperties null }}

@property
def additional_properties_(self, name: str) -> schemas.OUTPUT_BASE_TYPES:
    return self.__getitem__(name)
{{else}}
    {{#with additionalProperties}}
        {{#unless isBooleanSchemaFalse}}

@property
{{> components/schemas/_helper_schema_output_type mode="def_property" fullRefModule=null endChar=":" }}
        {{/unless}}
    {{/with}}
{{/eq}}