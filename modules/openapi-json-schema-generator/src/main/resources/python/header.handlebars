# coding: utf-8

{{>partial_header}}

from dataclasses import dataclass
import typing_extensions
import urllib3

from {{packageName}} import api_client, exceptions
{{#with header}}
{{> model_templates/imports }}
{{#if schema}}
    {{#with schema}}
from . import {{name.getSnakeCaseName}}
    {{/with}}
{{else}}
    {{#if getContent}}
        {{#each getContent}}
            {{#with this}}
                {{#with schema}}
from .content.{{../@key.getSnakeCaseName}} import {{name.getSnakeCaseName}}
                {{/with}}
            {{/with}}
        {{/each}}
    {{/if}}
{{/if}}


{{> parameter_instance noName=true }}
{{/with}}