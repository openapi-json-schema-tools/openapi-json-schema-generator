{{#each getRequiredProperties}}
{{#with this}}
{{#if refClass}}
{{@key}}: '{{refClass}}'
{{else}}
    {{#if name}}
        {{#if schemaIsFromAdditionalProperties}}
{{@key}}: MetaOapg.{{name.getName}}
        {{else}}
{{@key}}: MetaOapg.properties.{{name.getName}}
        {{/if}}
    {{else}}
{{@key}}: schemas.AnyTypeSchema
    {{/if}}
{{/if}}
{{/with}}
{{/each}}