{{#each getRequiredProperties}}
{{#with this}}
{{#if refClass}}
{{@key}}: '{{refClass}}'
{{else}}
    {{#if name}}
        {{#if schemaIsFromAdditionalProperties}}
{{@key}}: MetaOapg.{{name.name}}
        {{else}}
{{@key}}: MetaOapg.properties.{{name.name}}
        {{/if}}
    {{else}}
{{@key}}: schemas.AnyTypeSchema
    {{/if}}
{{/if}}
{{/with}}
{{/each}}