{{#each getRequiredProperties}}
{{#with this}}
{{#unless nameInSnakeCase}}
{{#if refClass}}
{{@key}}: '{{refClass}}'
{{else}}
    {{#if baseName}}
        {{#if schemaIsFromAdditionalProperties}}
{{@key}}: MetaOapg.{{baseName}}
        {{else}}
{{@key}}: MetaOapg.properties.{{baseName}}
        {{/if}}
    {{else}}
{{@key}}: schemas.AnyTypeSchema
    {{/if}}
{{/if}}
{{/unless}}
{{/with}}
{{/each}}