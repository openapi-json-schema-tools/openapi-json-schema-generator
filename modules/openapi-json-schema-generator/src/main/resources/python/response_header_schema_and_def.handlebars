

class {{xParamsName}}:
    RequiredParams = typing_extensions.TypedDict(
        'RequiredParams',
        {
{{#each xParams}}
{{> response_header_required_oneline }}
{{/each}}
        }
    )
    OptionalParams = typing_extensions.TypedDict(
        'OptionalParams',
        {
{{#each xParams}}
{{> response_header_optional_oneline }}
{{/each}}
        },
        total=False
    )


    class Params(RequiredParams, OptionalParams):
        pass


    parameters = [
    {{#each xParams}}
        {{#if refInfo.refModule}}{{refInfo.refModule}}{{else}}{{name.getSnakeCaseName}}{{/if}}.{{#if refInfo.refClass}}{{refInfo.refClass}}{{else}}{{name.getCamelCaseName}}{{/if}},
    {{/each}}
    ]