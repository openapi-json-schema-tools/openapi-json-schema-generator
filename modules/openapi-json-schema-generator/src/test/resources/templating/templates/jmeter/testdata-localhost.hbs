testCase,httpStatusCode{{#each operations}}{{#each .}}{{#if allParams}},{{/if}}{{#each allParams}}{{paramName}}{{#unless @last}},{{/unless}}{{/each}}{{/each}}{{/each}}
Success,200{{#each operations}}{{#each .}}{{#if allParams}},{{/if}}{{#each allParams}}0{{#unless @last}},{{/unless}}{{/each}}{{/each}}{{/each}}