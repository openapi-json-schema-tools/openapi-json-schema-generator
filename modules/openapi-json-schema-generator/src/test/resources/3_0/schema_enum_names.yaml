openapi: 3.0.3
info:
  title: Test
  version: 1.0.0-SNAPSHOT
paths:
  /test:
    get:
      tags:
        - Test Resource
      parameters:
        - name: uuid
          in: query
          schema:
            $ref: '#/components/schemas/UUID'
      responses:
        "200":
          description: OK
components:
  schemas:
    StringEnum:
      type: string
      enum:
        - "#1"
        - "#2"
    NumericEnum:
      type: number
      enum:
        - 1
        - 2