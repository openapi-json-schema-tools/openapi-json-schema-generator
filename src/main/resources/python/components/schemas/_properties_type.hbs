{{properties.jsonPathPiece.pascalCase}} = typing.TypedDict(
    '{{properties.jsonPathPiece.pascalCase}}',
    {
        {{#each properties}}
            {{#if refInfo.refClass}}
        "{{{@key.original}}}": typing.Type[{{#if refInfo.refModuleAlias}}{{refInfo.refModuleAlias}}.{{refInfo.refClass}}{{else}}{{#if refInfo.refModule}}{{refInfo.refModule}}.{{refInfo.refClass}}{{else}}{{! self reference }}'{{refInfo.refClass}}'{{/if}}{{/if}}],
            {{else}}
        "{{{@key.original}}}": typing.Type[{{jsonPathPiece.pascalCase}}],
            {{/if}}
        {{/each}}
    }
)
