@Override
public {{jsonPathPiece.pascalCase}}BoxedMap validateAndBox(Map<?, ?> arg, SchemaConfiguration configuration) throws ValidationException {
    return new {{jsonPathPiece.pascalCase}}BoxedMap(validate(arg, configuration));
}
