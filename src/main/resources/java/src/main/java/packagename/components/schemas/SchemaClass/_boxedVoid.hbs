{{#if forDocs}}
{{> src/main/java/packagename/components/_helper_header_from_identifier_pieces headerSize=(join headerSize "#" "") identifierPieces=(append identifierPieces (join jsonPathPiece.pascalCase "BoxedVoid" "")) }}
public static final class {{jsonPathPiece.pascalCase}}BoxedVoid<br>
implements [{{jsonPathPiece.pascalCase}}Boxed](#{{> src/main/java/packagename/components/_helper_anchor_id identifierPieces=(append identifierPieces (join jsonPathPiece.kebabCase "boxed" "")) }})

a boxed class to store validated null payloads, sealed permits class implementation

{{headerSize}}## Constructor Summary
| Constructor and Description |
| --------------------------- |
| {{jsonPathPiece.pascalCase}}BoxedVoid(Void data)<br>Creates an instance, private visibility |

{{headerSize}}## Field Summary
| Modifier and Type | Field and Description |
| ----------------- | ---------------------- |
| Void | data<br>validated payload |

{{else}}
public record {{jsonPathPiece.pascalCase}}BoxedVoid(Void data) implements {{jsonPathPiece.pascalCase}}Boxed {
    @Override
    public @Nullable Object getData() {
        return data;
    }
}

{{/if}}