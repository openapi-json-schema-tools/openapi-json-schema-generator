{{#if mapOutputJsonPathPiece}}
    {{#if mapValueSchema}}

@Override
protected {{mapOutputJsonPathPiece.camelCase}} getMapOutputInstance(FrozenMap<String, {{#with mapValueSchema}}{{> src/main/java/packagename/components/schemas/types/schema_output_type sourceJsonPath=../jsonPath forceNull=true }}{{else}}Object{{/with}}> arg) {
    return new {{mapOutputJsonPathPiece.camelCase}}(arg);
}
    {{else}}

@Override
protected {{mapOutputJsonPathPiece.camelCase}} getMapOutputInstance(FrozenMap<String, Object> arg) {
    return new {{mapOutputJsonPathPiece.camelCase}}(arg);
}
    {{/if}}
{{else}}

@Override
protected FrozenMap<String, Object> getMapOutputInstance(FrozenMap<String, Object> arg) {
    return arg;
}
{{/if}}
