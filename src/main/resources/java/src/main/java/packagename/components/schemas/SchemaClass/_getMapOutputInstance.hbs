{{#if mapOutputJsonPathPiece}}
    {{#if mapValueSchema}}

@Override
protected {{mapOutputJsonPathPiece.camelCase}} getMapOutputInstance(FrozenMap<String, ?> arg) {
    return new {{mapOutputJsonPathPiece.camelCase}}((FrozenMap<String, {{#with mapValueSchema}}{{> src/main/java/packagename/components/schemas/types/schema_output_type forceNull=true }}{{/with}}>) arg);
}
    {{else}}

@Override
protected {{mapOutputJsonPathPiece.camelCase}} getMapOutputInstance(FrozenMap<String, ?> arg) {
    return new {{mapOutputJsonPathPiece.camelCase}}((FrozenMap<String, Object>) arg);
}
    {{/if}}
{{/if}}
