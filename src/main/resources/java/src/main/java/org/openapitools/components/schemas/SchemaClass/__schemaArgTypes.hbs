{{#each schemaKeywords}}
    {{#eq this "type"}}
LinkedHashSet<Class<?>> type{{#unless @last}},{{/unless}}
    {{else}}
        {{#eq this "format"}}
String format{{#unless @last}},{{/unless}}
        {{else}}
            {{#eq this "items"}}
Class<?> items{{#unless @last}},{{/unless}}
            {{else}}
                {{#eq this "properties"}}
LinkedHashMap<String, Class<?>> properties{{#unless @last}},{{/unless}}
                {{else}}
                    {{#eq this "required"}}
Set<String> required{{#unless @last}},{{/unless}}
                    {{else}}
                        {{#eq this "additionalProperties"}}
Class<?> additionalProperties{{#unless @last}},{{/unless}}
                        {{else}}
Void {{this}}{{#unless @last}},{{/unless}}
                        {{/eq}}
                    {{/eq}}
                {{/eq}}
            {{/eq}}
        {{/eq}}
    {{/eq}}
{{/each}}