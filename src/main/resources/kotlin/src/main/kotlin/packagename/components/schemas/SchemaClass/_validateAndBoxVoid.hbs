@Override
public {{jsonPathPiece.pascalCase}}BoxedVoid validateAndBox(Nothing? arg, SchemaConfiguration configuration) throws ValidationException {
    return new {{jsonPathPiece.pascalCase}}BoxedVoid(validate(arg, configuration));
}
