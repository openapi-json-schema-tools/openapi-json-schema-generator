{{#if forDocs}}
required = setOf(<br>
    {{~#each requiredProperties}}
&nbsp;&nbsp;&nbsp;&nbsp;"{{{@key.original}}}"{{#unless @last}},{{/unless}}<br>
    {{~/each}}
)<br>
{{~else}}
.required(setOf(
    {{#each requiredProperties}}
    "{{{@key.original}}}"{{#unless @last}},{{/unless}}
    {{/each}}
))
{{/if}}